import { ThumbTemperatureDirective } from './thumb-temperature.directive';

describe('ThumbTemperatureDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbTemperatureDirective();
    expect(directive).toBeTruthy();
  });
});
