pipeline {
    agent any
    stages {
        stage('deploy') {
            steps {
                C:\Program Files\Git\bin sh '''
                npm install -g @angular/cli
                npm install
                npm deploy
                '''
            }
        }
    }
}