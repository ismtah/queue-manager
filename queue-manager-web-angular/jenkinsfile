pipeline {
    agent any
    stages {
        stage('deploy') {
            steps {
                sh '''
                npm install -g @angular/cli
                npm install
                npm deploy
                '''
            }
        }
    }
}